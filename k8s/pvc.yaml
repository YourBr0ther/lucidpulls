apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lucidpulls-data
  namespace: lucidpulls
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path  # k3s default storage class
  resources:
    requests:
      storage: 1Gi
